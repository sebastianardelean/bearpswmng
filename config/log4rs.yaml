refresh_rate: 30 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d} [{f}:{L}] {m}{n}"
  requests:
    kind: rolling_file
    path: "log/trace.log"
    policy:
      kind: compound
      trigger:
        kind: size
        limit: 10mb   # rotate when log exceeds 10 MB
      roller:
        kind: fixed_window
        pattern: "log/trace.log.{}"
        base: 1
        count: 5      # keep 5 old log files
    encoder:
      pattern: "{d} [{f}:{L}] {m}{n}"
root:
  level: warn
  appenders:
    - stdout
    - requests
loggers:
  app::backend::db:
    level: warn
  app::requests:
    level: warn
    appenders:
      - requests
    additive: true
